# Local curated sources

Curated source workbooks may be placed in this directory for local imports, but the workbook itself is intentionally not tracked until redistribution rights and file size are reviewed. Use `tools/import_curated_modifications.py` to generate `data/modifications.yaml` and an auditable diff report.
